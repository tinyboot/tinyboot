import { Identifier } from '../../common/identifier';

export interface MethodParameterMetadata {
  /**
   * The identifier of the parameter provider.
   * @type  {Identifier}
   * @memberof MethodParameterMetadata
   */
  identifier?: Identifier;
}
